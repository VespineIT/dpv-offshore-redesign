/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      // No remote domains needed as we are using local /public images
    },
  };
  
  module.exports = nextConfig;